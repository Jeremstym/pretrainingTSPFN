defaults:
  - default

_target_: data.data_module.EICUFineTuneDataModule
data_roots: ${oc.env:EICU_DATA_PATH}
time_series_length: 100
num_channels: 5
num_classes: 2

predict_losses:
  classification:
    _target_: torch.nn.BCEWithLogitsLoss