defaults:
  - default

_target_: data.data_module.CPSCDataModule
data_roots: ${oc.env:CPSC_DATA_PATH}
time_series_length: 125
time_points: null
num_channels: null
num_classes: 4

predict_losses:
  classification:
    _target_: torch.nn.CrossEntropyLoss