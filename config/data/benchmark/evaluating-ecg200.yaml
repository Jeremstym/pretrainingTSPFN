defaults:
  - default

_target_: data.benchmark_data_module.ECG200DataModule
data_roots: ${oc.env:ECG200_DATA_PATH}
time_series_length: 96
time_points: null
num_channels: null
num_classes: 2

predict_losses:
  classification:
    _target_: torch.nn.CrossEntropyLoss